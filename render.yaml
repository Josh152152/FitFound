services:
  - type: web
    name: ai-talent-marketplace
    env: python
    buildCommand: |
      pip install --upgrade setuptools wheel pip  # Force install build tools first
      pip install -r requirements.txt            # Install other dependencies
    startCommand: "python run.py"
